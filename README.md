### Arch Linux

```bash
sudo pacman -Syu
sudo pacman -S --needed base-devel git gdb pkgconf sdl3
```

### Ubuntu

```bash
sudo apt update
sudo apt install build-essential git gdb pkg-config libsdl3-dev
```
